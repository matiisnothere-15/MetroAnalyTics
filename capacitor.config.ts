import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'ionic.metroanalytics',
  appName: 'metroanalytics',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
